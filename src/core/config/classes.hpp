
class Game {
	class Constructor {
		code = "call core_fnc_gameInit";
	};
	capTimeBase = 0.5; // Base cap time (if only one person in the zone then * 100 to get the total cap time) (Min 0.1)
};

class Team {

};